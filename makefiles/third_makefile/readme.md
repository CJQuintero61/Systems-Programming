This directory simplifies the makefile even further

NOTE:: using 'make -jN' you can run the makefile in parallel
with N number of jobs/processes. Ex) 'make -j8' to run 8 processes

to run in a unix terminal:
    1. 'make'
    2. './main'
    3. 'make clean'